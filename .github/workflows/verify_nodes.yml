
name: 🤖 PKRD Node Verifier

on:
  push:
    paths:
      - "node_requests/**"

jobs:
  verify-nodes:
    runs-on: ubuntu-latest
    name: Run PKRD Node Verifier
    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v3

      - name: 🔧 Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.11

      - name: 🧪 Install Python Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install eth_account eth_utils

      - name: 🤖 Run Verifier Script
        run: ./PKRD-NODES
  run: |
          python verify_nodes.py

      - name: 📤 Commit Approved Nodes
        run: |
          git config --global user.name "PKRD AI Bot"
          git config --global user.email "bot@pkr.digital"
          git add approved/
          git commit -m "✅ Approved new nodes via AI" || echo "No changes to commit"
          git push
